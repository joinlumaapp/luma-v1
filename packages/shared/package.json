{
  "name": "@luma/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and constants for LUMA V1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  }
}
